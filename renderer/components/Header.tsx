import { CardHeader, Heading } from "@chakra-ui/react";
import React from "react";

const Header = () => {
  return <Heading textAlign={"center"}>Roman Numeral Converter</Heading>;
};

export default Header;
